services:
  - type: web
    name: thesis-chatbot-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m app.main
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false  # You'll set this manually in Render dashboard 